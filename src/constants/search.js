// Failed api
export const FAILED = 'SEARCH_FAILED';

// wait api return data
export const LOAD = 'SEARCH_LOAD';

// api return data
export const OK = 'SEARCH_OK';

// sort column datagrid
export const SORT = 'SEARCH_SORT';

// Filtering data
export const FILTER = 'SEARCH_FILTER';

// infinite scroll
export const ADD = 'SEARCH_ADD';

// api return 0 data
export const EMPTY = 'SEARCH_EMPTY';
